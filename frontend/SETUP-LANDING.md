# CivicPulse Landing Page

Replace the corresponding files in `frontend/src/`.

Install React Router if needed:

```bash
npm install react-router-dom
```

Then run:

```bash
npm run dev
```

The page is at `/`.

The report/login/municipal routes are temporary placeholders so the landing-page CTAs work without requiring the rest of the application yet.
